const RUNTIME_CONFIG_FILE = 'crc.json'

const DEFAULT_RUNTIME_CONFIGURATION = {
    createDir: true,
    typescript: false,
    includeIndex: true,
    type: 'functional',
}

module.exports = {
    RUNTIME_CONFIG_FILE,
    DEFAULT_RUNTIME_CONFIGURATION,
}
